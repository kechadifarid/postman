import type { Label, Link } from "allure-js-commons";
import type { EventList } from "postman-collection";
export declare const extractMeta: (eventList: EventList) => {
    labels: Label[];
    links: Link[];
};
